# Configuración principal del orquestador
directories_to_process:
  - dir1
  - dir2
  - dir3
  - dir4

calculation_script: "your_script.py"

resource_config:
  cores_to_use: 8  # Opcional: por defecto usará todos los disponibles

log_config:
  log_file: "orchestrator.log"
  log_level: "INFO"   # Valores posibles: DEBUG, INFO, WARNING, ERROR, CRITICAL